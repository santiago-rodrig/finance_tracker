document.addEventListener('ajax:success', function() {
  document.getElementById('messages').innerHTML = '<%= j(render('layouts/messages')) %>';
  <% if @stock %>
      document.getElementById('result').innerHTML = '<%= j(render('users/result.html')) %>';
  <% end %>
});

